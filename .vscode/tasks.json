{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${workspaceFolder}/A星寻路算法.out"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
